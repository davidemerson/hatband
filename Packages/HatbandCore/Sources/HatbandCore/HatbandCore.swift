/// Shared model, codec, crypto and QR code for Hatband.
/// Builds on Linux and Apple platforms; the app links it unchanged.
public enum HatbandCore {
    /// Version of the HB1 wire format this library reads and writes.
    public static let formatVersion: UInt8 = 1
}
